apiVersion: v1
kind: Service
metadata:
  name: {{ template "mifos.fullname" . }}
  labels:
    app: {{ template "mifos.name" . }}
    chart: {{ template "mifos.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mifos.name" . }}
    release: {{ .Release.Name }}
